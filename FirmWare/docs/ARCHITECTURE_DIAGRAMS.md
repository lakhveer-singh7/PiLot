# PiLot Shared Memory Architecture - Visual Diagrams

This document provides ASCII art diagrams to visualize the shared memory communication architecture.

## 1. System Overview - Device Topology

```
┌────────────────────────────────────────────────────────────────────────────┐
│                           PiLot Distributed CNN                            │
│                        12 nRF52840 Devices (256KB RAM)                     │
└────────────────────────────────────────────────────────────────────────────┘

         ┌─────────┐
         │  Head   │  Device 0: Dataset Loader + Pipeline Coordinator
         │ (Dev 0) │  - Loads UCR time series samples
         └────┬────┘  - Orchestrates pipeline phases
              │       - Signals PHASE_FORWARD_LAYER* and PHASE_BACKWARD_LAYER*
              ↓
      ┌───────────────┐
      │ SHM_LAYER0_IN │  Shared Memory: 1×300 floats (raw input)
      │   Key: 0x1234  │
      └───────┬───────┘
              ↓
         ┌────────┐
         │ Layer0 │  Device 1: Conv1D (1→16 channels)
         │ (Dev1) │  - Single worker processes entire input
         └────┬───┘
              ↓
      ┌───────────────┐
      │ SHM_LAYER1_IN │  Shared Memory: 16×300 floats
      │   Key: 0x1235  │
      └───────┬───────┘
              ↓
    ┌─────────┴─────────┐
    │                   │
┌───┴───┐          ┌────┴────┐
│Layer1 │          │ Layer1  │  Devices 2-3: Conv1D (16→16 each)
│ W0    │          │ W1      │  - 2 workers, each outputs 16 channels
│(Dev2) │          │ (Dev3)  │  - Combined output: 32 channels
└───┬───┘          └────┬────┘
    └─────────┬─────────┘
              ↓
      ┌───────────────┐
      │ SHM_LAYER2_IN │  Shared Memory: 32×300 floats
      │   Key: 0x1236  │
      └───────┬───────┘
              ↓
    ┌─────────┴──────────────┐
    │                        │
┌───┴───┐ ┌────────┐    ┌────┴────┐
│Layer2 │ │ Layer2 │    │ Layer2  │  Devices 4-6: Conv1D (32→16 each)
│ W0    │ │ W1     │    │ W2      │  - 3 workers, each outputs 16 channels
│(Dev4) │ │ (Dev5) │    │ (Dev6)  │  - Combined output: 48 channels
└───┬───┘ └────┬───┘    └────┬────┘
    └──────────┴─────────────┘
              ↓
      ┌───────────────┐
      │ SHM_LAYER3_IN │  Shared Memory: 48×300 floats
      │   Key: 0x1237  │
      └───────┬───────┘
              ↓
    ┌─────────┴────────────────────┐
    │                              │
┌───┴───┐ ┌────────┐ ┌────────┐ ┌─┴────┐
│Layer3 │ │ Layer3 │ │ Layer3 │ │Layer3│  Devices 7-10: Conv1D (48→16 each)
│ W0    │ │ W1     │ │ W2     │ │ W3   │  - 4 workers, each outputs 16 channels
│(Dev7) │ │ (Dev8) │ │ (Dev9) │ │(D10) │  - Combined output: 64 channels
└───┬───┘ └────┬───┘ └────┬───┘ └──┬───┘
    └──────────┴───────────┴────────┘
              ↓
      ┌───────────────┐
      │ SHM_LAYER4_IN │  Shared Memory: 64×300 floats
      │   Key: 0x1238  │
      └───────┬───────┘
              ↓
         ┌────────┐
         │  Tail  │  Device 11: Classifier
         │ (Dev11)│  - Dual Pooling (Avg+Max): 64 → 128 features
         └────────┘  - Fully Connected: 128 → 12 classes
                     - Softmax + Cross-Entropy Loss
```

---

**Document Version:** 1.0  
**Last Updated:** February 17, 2026  
**Companion to:** SHARED_MEMORY_ANALYSIS.md
